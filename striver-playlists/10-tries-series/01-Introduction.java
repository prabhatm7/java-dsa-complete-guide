/*
 * Problem: Trie introduction — prefix tree data structure.
 *
 * Intuition: Each node has 26 child links; flag end-of-word at terminating node.
 * Time: O(L) per op  Space: O(N*L)
 */
public class Introduction {}
